# Where the NestJS gateway is listening.
# Must appear in the gateway's CORS_ORIGINS, and this app must run on a port
# that list allows — hence `next dev -p 3001`.
NEXT_PUBLIC_GATEWAY_URL=http://localhost:3000
